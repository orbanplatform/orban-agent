# Orban Agent Web UI

> 基于 Orban 品牌设计指导的 GPU 算力贡献平台前端界面

## 🎨 设计规范

### 品牌色彩
- **主色调**: `#6FBD58` (Orban Green)
- **辅助色**: `#FEC804` (Orban Yellow)
- **渐变**: 135° 从绿到黄

### 视觉特点
- ✨ 绿黄渐变背景
- 🎯 品牌色 Logo 图标
- 📊 渐变进度条和数据展示
- 💫 呼吸脉冲动画
- 🎴 渐变文字效果

## 🚀 快速启动

```bash
# 进入 web-ui 目录
cd web-ui

# 启动本地服务器
python3 -m http.server 8080

# 在浏览器中访问
open http://localhost:8080
```

## 📊 功能模块

### 1. 收益概览
- 总收益、今日收益、待确认收益
- 完成任务数统计
- 实时更新（基于真实定价模型）

### 2. GPU 监控
- 多 GPU 卡片展示
- 实时使用率、内存、温度、功耗
- 每 5 秒自动刷新

### 3. 收益历史
- 表格形式展示历史记录
- 时间、GPU 型号、执行时长、费率
- 状态标识（已确认/待确认）

### 4. Agent 信息
- Agent ID、版本、运行时长
- 平台连接状态

## 💰 真实定价

根据 Orban 实际定价模型：

| GPU 型号 | 费率 | 每日收益 | 每月收益 |
|---------|------|---------|---------|
| RTX 4090 | $0.025/h | $0.60 | $18.00 |
| RTX 3090 | $0.018/h | $0.43 | $12.96 |

当前展示数据：
- 总收益: $8.45（约 2 周累计）
- 今日收益: $0.58（RTX 4090 运行 24h）
- 单次任务: $0.001-$0.005（3-12 分钟）

## 🔄 数据更新

目前使用模拟数据展示界面效果：
- GPU 状态每 5 秒更新
- 运行时长每秒更新
- 新收益记录每 10 秒添加

## 🛠️ 技术栈

- **纯前端**: HTML5 + CSS3 + Vanilla JavaScript
- **无框架**: 零依赖，轻量快速
- **响应式**: 支持桌面和移动端
- **模块化**: 易于集成到 Tauri/Electron

## 📁 文件结构

```
web-ui/
├── index.html          # 主页面
├── css/
│   └── style.css      # Orban 品牌样式
├── js/
│   └── app.js         # 应用逻辑和模拟数据
└── README.md          # 本文档
```

## 🔌 后端集成

未来可以通过以下方式连接真实数据：

### 方案 1: HTTP API
```javascript
async function fetchGPUStatus() {
    const response = await fetch('http://localhost:8000/api/gpu/status');
    return await response.json();
}
```

### 方案 2: WebSocket
```javascript
const ws = new WebSocket('ws://localhost:8000/ws');
ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    updateUI(data);
};
```

### 方案 3: Tauri IPC
```javascript
import { invoke } from '@tauri-apps/api/tauri';
const gpuStatus = await invoke('get_gpu_status');
```

## 🎯 下一步优化

- [ ] 添加图表展示（收益趋势、GPU 使用率曲线）
- [ ] 实现暗色模式切换
- [ ] 添加设置面板（配置 Agent 参数）
- [ ] 集成 Rust 后端 API
- [ ] 迁移到 Tauri 框架
- [ ] 添加通知系统

## 📄 许可证

MIT License - 与 Orban Agent 主项目保持一致
